Trains an AI model for predicting sentiments from 10 000 restaurant reviews. User can then write a review and see the predicted sentiment.

## Install the requirements:

    pip install -r requirements.txt

## Start the app:

    streamlit run app.py
